<Window x:Class="coursework.Views.ABTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="A/B Тестування планів фестивалю" Height="550" Width="850"
        WindowStartupLocation="CenterOwner" Background="#F4F6F9">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="A/B Порівняння конфігурацій" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="ПЛАН А (Контрольний)" FontSize="14" FontWeight="Bold" Foreground="#3498DB" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding File1Name}" Foreground="#7F8C8D" Margin="0,0,0,10"/>
                    <Button Content="📁 Вибрати JSON" Command="{Binding LoadFile1Command}" Background="#3498DB" Foreground="White" Height="30" BorderThickness="0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="ПЛАН Б (Тестовий)" FontSize="14" FontWeight="Bold" Foreground="#E74C3C" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding File2Name}" Foreground="#7F8C8D" Margin="0,0,0,10"/>
                    <Button Content="📁 Вибрати JSON" Command="{Binding LoadFile2Command}" Background="#E74C3C" Foreground="White" Height="30" BorderThickness="0"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="5" Opacity="0.1"/>
            </Border.Effect>
            <lvc:CartesianChart Series="{Binding ComparisonChart}" LegendLocation="Right">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Labels="{Binding Labels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis LabelFormatter="{Binding Formatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Border>

        <Border Grid.Row="3" Background="#EAFAF1" BorderBrush="#2ECC71" BorderThickness="2" CornerRadius="8" Padding="15" Margin="0,20,0,0">
            <TextBlock Text="{Binding Conclusion}" FontSize="16" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>